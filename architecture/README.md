# architecture

Model definitions and architecture-level components: layers, attention variants, routing, and other structural innovations that increase capability density per parameter. This module includes subdirectories for `moe` and `attention`.

See the top-level `MISSION.md` for design rationale.